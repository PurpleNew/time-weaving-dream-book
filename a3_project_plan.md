# 我对 A3「迹点」选题的理解与 15 天实现计划

我看完题目后的第一反应是：这不是一个普通日历，而是一个“帮我把长期目标拆成每天能执行的任务，并且能跟着进度走”的智能时间管理平台。

如果我把题目拆开看，它真正想解决的不是“记日程”这么简单，而是下面三件事：

1. 把长线目标拆成子任务。
2. 把日程和进度看清楚。
3. 让 AI 帮我减少规划成本。

我们只有两个人，而且都是初学者，所以我不能一上来就想做一个“什么都有”的完整产品。我的策略应该是先做一个稳定、能演示、能讲清楚故事的 MVP，再在这个基础上补加分项。

---

## 一、我先给这个题目下一个最现实的判断

这个项目里，真正值得优先做出来的核心功能是：

- 手动添加日程
- 长线目标拆分成子任务
- 子任务完成状态可视化
- AI 帮我生成子任务
- 空闲时间推荐安排
- 每日总览/日报

这些做出来，就已经能把题目的主线讲通了。

我不建议一开始就碰太重的东西，比如：

- 企微、公众号、QQBot 的完整接入
- CalDav、Exchange 的完整联动
- 原生 App 多端同步
- 复杂的 DAG 排程算法

这些不是不能做，而是对我们现在的时间和能力来说，风险太高。比赛要的是完成度和展示效果，不是把所有接口都堆满。

所以我的原则是：

- 先做“能跑起来”的版本
- 再做“能展示亮点”的版本
- 最后补“加分项”

---

## 二、需要学什么，学到什么程度

我不需要把所有技术都学到很深。我只需要学到“能把项目做出来”的程度。

| 方向 | 我需要学什么 | 学到什么程度 |
|---|---|---|
| Git / 协作 | commit、branch、merge、解决简单冲突 | 能和队友正常协作，不会互相覆盖代码 |
| HTTP / API | GET、POST、PUT、DELETE、JSON、状态码 | 能看懂前后端接口并自己写简单接口 |
| 前端框架 | 组件、表单、路由、状态管理、调用接口 | 能做页面、表单、列表、弹窗、详情页 |
| 后端框架 | 路由、参数校验、文件上传、返回 JSON | 能提供日程增删改查接口 |
| 数据库 | 表设计、主键、外键、增删改查 | 能存事件、子任务、提醒、导入内容 |
| AI 调用 | 提示词、结构化输出、错误重试 | 能把自然语言变成结构化日程数据 |
| OCR / 文本提取 | 截图识别、文本清洗、脱敏 | 能把截图里的文字提出来并转成日程 |
| 可视化 | 时间轴、进度条、图表组件 | 能把进度和 ddl 讲清楚 |
| 部署 | 本地运行、环境变量、打包 | 能在比赛现场稳定演示 |

### 我不需要学到很深的部分

- 我不需要自己训练模型
- 我不需要自己从零写 OCR 算法
- 我不需要做复杂的调度优化论文算法
- 我不需要把安全体系做成企业级

因为这次题目更偏“AI 应用 + 产品落地”，不是“算法竞赛”。我需要的是会用现成能力，把它们整合成一个完整产品。

---

## 三、哪些可以靠 AI，哪些必须我自己学

### 可以交给 AI 帮我很多的部分

- 生成前端页面初稿
- 生成后端接口模板
- 帮我写页面文案和按钮文案
- 帮我把需求变成数据库字段建议
- 帮我写提示词初稿
- 帮我生成测试数据
- 帮我检查代码报错思路
- 帮我润色答辩稿

### 不能完全依赖 AI 的部分

- 需求理解
- 功能取舍
- 数据结构设计
- 页面流程设计
- 接口是否符合项目逻辑
- AI 输出是否正确
- 最终集成和排错

我的态度应该是：AI 可以当我的助理，但不能替我做决定。  
我必须先知道自己要什么，再让 AI 帮我提速。

---

## 四、我对题目里每个功能点的拆解

### 1. 长线目标拆解

题目说的“长线目标”，本质上就是一个父任务，下面挂多个子任务。

我理解成：

- 父任务：比如“期末复习”
- 子任务：比如“第 1 周看完第 1 章”“刷完 3 套题”“整理错题”

我需要做的不是复杂算法，而是把这个关系设计清楚：

- 父任务有标题、开始时间、截止时间、进度
- 子任务有完成状态、时间安排、是否提醒
- 子任务完成后，父任务自动更新进度

这个功能的关键，不是技术难度，而是产品逻辑要顺。

### 2. 多源日程整合

题目里提到可以从教务系统导入课程表、手动添加任务、识别截图、接收聊天记录等。

如果时间很紧，我不会把所有渠道都做全。我会分成三层：

1. 第一层：手动添加日程
2. 第二层：上传截图或粘贴文本，AI 帮我提取日程
3. 第三层：支持 `.ics` 导入导出

这样最稳。

我不建议一开始就做所有平台接入，因为这些接口会带来很多认证、格式和联调问题，很容易把时间吃光。

### 3. 进度可视化

这个功能是比赛里很重要的展示点。

我需要做的是把“做了多少”清楚地展示出来，比如：

- 时间轴
- 进度条
- 任务完成率
- 未完成任务标红
- ddl 预警

这个部分不需要特别高深，重点是让评委一眼看懂：

“这个平台真的能帮助我管理长线任务。”

### 4. 空闲时间利用

这部分本质上是一个简单的推荐逻辑，不是特别复杂的智能优化。

我可以这样做：

- 找出日程中的空白时间段
- 看看有没有没完成的子任务
- 根据任务时长和截止时间，推荐插入合适的任务

也就是说，我不需要做一个很复杂的调度系统，只要能给出“今天 14:00 到 15:00 可以补做英语背诵”这种建议，就已经够用了。

### 5. 多渠道添加日程

题目里写了很多入口，比如截图、企微、公众号、QQBot、URL、CalDav、Exchange。

我的理解是：题目想要的是“日程来源很多，但最后都要统一成一个结构化数据模型”。

所以我的实现思路应该是：

- 外部输入先转成文本
- 再把文本清洗成统一格式
- 最后存进数据库

这样我只需要维护一种内部结构，不用为每个入口单独做一套业务逻辑。

### 6. 多样化事件形式

这个功能的核心是：不只支持普通日程，还要支持“长线事件”和“弱日程”。

我可以把事件简单分成三类：

- 普通日程：某个时间点开始、结束
- 长线事件：一个大目标，下面有多个子任务
- 弱日程：暂时没完全定死，但先放进计划里

这样做的好处是结构很清楚，也方便展示“日程不是死的，而是可以逐步完善的”。

### 7. AI 规划

这是整个题目的核心亮点之一。

题目要求 AI 能做的事情有三类：

1. 根据课程大纲、任务要求，帮我规划子日程
2. 根据手写/电子规划，把内容结构化成子任务
3. 根据完成度和空闲时间，动态调整后续安排

我理解后，最适合我们初学者的做法不是训练模型，而是：

- 让 AI 输出严格的结构化结果
- 我们再把结果存到系统里
- 如果格式不对，就让它重试

也就是说，AI 不是直接“做决定”，而是先“生成建议”，再由系统接管落地。

### 8. 可视化页面

我需要至少做出这几个页面：

- 首页 / 日历页
- 长线目标详情页
- AI 规划页
- 看板页
- 日报页

这些页面不一定要很复杂，但一定要有完整的任务流：

“我创建目标 -> AI 拆分 -> 看板跟进 -> 日报提醒 -> 完成反馈”

只要这条线能跑通，整个产品就成立了。

### 9. 隐私与安全

题目里提到本地预处理和敏感信息脱敏，这一点我不能完全忽略。

我可以先做最基础的处理：

- 识别手机号、身份证号、邮箱等敏感信息
- 在发送给 AI 前做简单掩码
- 不把不必要的原始信息直接上传

这部分不需要做得很重，但答辩时一定要能讲出来。

### 10. 其它辅助建议

题目还提到天气、道路状况、公共交通等辅助决策要素。

这个部分我可以做成“增强建议”，不是主功能。

比如：

- 下雨天不建议安排户外任务
- 如果通勤时间太长，提醒提前出门
- 如果某一天任务太多，提醒我分散到后面几天

这样就够了，不需要做成完整的交通规划系统。

### 11. 加分项

题目里的加分项很诱人，但我必须分清主次。

我可以把它理解成：

- DAG = 任务依赖图
- iCalendar = 标准日历导入导出
- 开放 API = 以后可扩展

如果时间够，我可以加一个简单的依赖关系展示，比如“任务 A 完成后才能做任务 B”。

但如果时间不够，我不会强行做复杂版本，因为那样很容易拖垮整个项目。

---

## 五、我和枫枫应该怎么分工

因为我们是两个人，而且都不是特别熟练，所以分工一定要简单明确。

### 我负责的部分

- AI 规划逻辑
- 提示词设计
- 结构化输出处理
- OCR / 文本清洗
- 空闲时间推荐逻辑
- 需求整理和答辩故事线

### 枫枫负责的部分

- 前端页面搭建
- 页面排版和交互
- 路由和页面跳转
- 图表和时间轴展示
- 基础后端接口联调

### 我们两个人都要做的部分

- 需求确认
- 数据结构讨论
- Bug 修复
- 联调测试
- 演示稿和答辩稿

我不希望出现“谁都以为对方在做”的情况，所以最开始就要把接口和页面列表定下来。

---

## 六、我建议我们采用的最小技术方案

如果从零开始，而且只有 15 天，我建议我们做 Web 端，不做原生 App。

一个比较稳的组合是：

- 前端：Vue 3 + Vite + Element Plus
- 后端：FastAPI
- 数据库：SQLite
- 可视化：ECharts
- AI：调用现成大模型 API
- OCR：用成熟开源库或现成识别接口

我这样选的原因很简单：

- 上手快
- 资料多
- 能快速出页面
- 适合做比赛 demo

如果我们更熟 JavaScript，也可以把后端换成 Node.js，但我不建议在这个阶段频繁换技术栈。

---

## 七、我需要额外去查什么

我不应该乱学，我只需要围绕项目查这些东西：

- Vue 3 或 React 的基础用法
- FastAPI 的接口写法
- SQLite 基础建表
- 文件上传接口
- 图表库怎么画时间轴和进度条
- 大模型 API 怎么返回 JSON
- OCR 怎么提取图片文字
- `.ics` 文件的基本格式
- Git 协作和冲突解决

我查资料的时候也要有顺序：

1. 先查能把 MVP 做出来的东西
2. 再查能提升展示效果的东西
3. 最后才查加分项

这样不会越学越散。

---

## 八、我给自己定的 15 天时间规划

### 第 1-2 天：定方案

- 确认 MVP
- 确认技术栈
- 画出页面草图
- 设计数据库表
- 列出接口清单

目标是：不再停留在“看不懂题目”，而是变成“知道先做什么”。

### 第 3-5 天：搭基础框架

- 前端搭页面骨架
- 后端搭接口骨架
- 数据库先跑通
- 完成日程增删改查

这三天最重要的是把基础链路打通。

### 第 6-8 天：做核心亮点

- 长线目标拆分
- 子任务完成状态
- AI 生成子任务
- 看板和进度展示

这一步是项目的主心骨。

### 第 9-10 天：做智能推荐

- 空闲时间推荐
- 今日总览
- 任务提醒
- ddl 预警

这部分会让项目看起来更“智能”。

### 第 11-12 天：补输入能力

- 截图识别 / 文本提取
- 简单脱敏
- `.ics` 导入导出

如果时间不够，这部分至少保留一个入口能演示。

### 第 13 天：修 Bug

- 清理接口错误
- 修页面样式
- 修逻辑漏洞
- 测试极端情况

### 第 14 天：做展示包装

- 准备答辩稿
- 准备演示数据
- 准备操作路径
- 准备备份方案

### 第 15 天：最终彩排

- 全流程演示
- 修最后的报错
- 确保现场能跑

我会把这一天当成“保命日”，不再加新功能。

---

## 九、我觉得最容易翻车的地方

### 1. 需求太大

题目功能很多，如果我什么都想做，最后很可能一个都做不稳。

### 2. AI 输出不稳定

如果我让 AI 自由发挥，它输出的格式可能会乱。

所以我一定要让它输出结构化结果，比如固定字段、固定格式。

### 3. 前后端联调拖时间

页面做出来不等于系统能跑通。

所以我必须尽早联调，不要等到最后两天才把接口接起来。

### 4. 过度追求加分项

加分项很好看，但基础功能不稳的话，加分项没有意义。

---

## 十、我最终想做成什么样

我希望最后展示出来的，不是一个“普通日历”，而是这样一个故事：

1. 我输入一个长线目标
2. 系统帮我拆成子任务
3. 我能在看板里看到进度
4. 系统能提醒我今天该做什么
5. 空闲时间还能主动推荐任务

如果我能把这条故事线讲完整，就算功能不是最多，项目也会显得完整而且有价值。

---

## 十一、我给自己的结论

这次项目对我来说，最重要的不是把所有技术都学会，而是学会“围绕一个目标，把能做出来的功能做扎实”。

我需要的不是一个完美系统，而是一个：

- 有明确场景
- 有完整流程
- 有 AI 亮点
- 能稳定演示

的作品。

只要我把核心链路做通，这个项目就不是一个简单的日历，而是一个真正能帮助学生管理长期任务的智能平台。
